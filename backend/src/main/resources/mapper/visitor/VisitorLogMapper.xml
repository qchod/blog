<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="click.bcyeon.back01.visitor.mapper.VisitorLogMapper">

    <insert id="insertVisitorLog">
        INSERT INTO visitor_log (
            ip, uri, method, status_code, response_time,
            user_agent, referer, country, city, visited_at
        ) VALUES (
            #{ip}, #{uri}, #{method}, #{statusCode}, #{responseTime},
            #{userAgent}, #{referer}, #{country}, #{city}, #{visitedAt}
        )
    </insert>

</mapper>
